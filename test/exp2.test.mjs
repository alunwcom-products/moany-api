// import {
//   expect, jest, test, beforeAll, describe
// } from '@jest/globals';

// import { generateJWT, verifyJWT, authenticate } from './utilities/auth.js';

// describe('tests', () => {
  beforeAll(async () => {
    // const fetchSpy = jest.spyOn(globalThis, 'fetch');
    // fetchSpy.mockImplementation((input) => {
    //   const found = okRequests.find((url) => url === input.url);
    //   if (found) {
    //     return new Response('Mocked success!', { status: 200 });
    //   }
    //   return new Response('Mocked not found!', { status: 404 });
    // });
  });


  test('simple test 2', async () => {
    // console.log('Running simple test');
    // const result = await authenticate('testuser', 'testpassword');
    expect(true).toBe(true);
  });

// });

